import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF6C63FF); // Main primary color (Soft Purple)
  static const Color customWhite = Color(0xFFF7F9FB); // A soft, warm white (Off White)
  static const Color deleteColor = Color(0xFFE57373); // A contrasting coral red (Soft Red)
  static const Color customChipColor = Color(0xFF81C784); // A soft green (Light Green)
  // Use a non-const variable to define a color with opacity
  static Color boxShadowColor = Colors.black.withOpacity(0.1); // Light shadow color (Soft Black)
  static const Color backgroundColor = Color(0xFFFAFAFA); // Background color (Light Grey)
  
}
